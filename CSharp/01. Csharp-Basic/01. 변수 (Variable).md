




string 관련 함수
```csharp
string str = "아무문자열~";

char[] arrayChar = str.ToCharArray();      // char 배열 형태로 변경
bool isContained = str.Contains("문");      // 특정 문자열이 포함되어 있는지 확인

string upperStr = str.ToUpper();          // 대문자 변환
string lowerStr = str.ToLower();          // 소문자 변환

```

